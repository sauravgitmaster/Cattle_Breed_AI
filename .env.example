# Local development can use the Vite proxy, so leave this empty.
# For Vercel, set this to the public Render API URL, without a trailing slash.
VITE_API_BASE_URL=https://your-render-service.onrender.com
